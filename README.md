## How to use the Terraform Repository

This terromm repository contains root and child modules.

The root modules are set of reusable teraform template that can be consumed to create a infrastructure
